#!/usr/bin/env python3.6

def gat():
	hei = float(input("what is ur hioght "))
	wei = float(input("what is ur weight "))
	sys = input("are ur meas in metric or imp ").lower().strip()

def calc(wei, hei, sys=metric'metric'):
	if sys == 'metric':
